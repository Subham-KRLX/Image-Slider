import React from 'react';
import '../App.css';

const Header = () => {
    return (
        <div className="header">
            {/* Header content */}
        </div>
    );
};

export default Header;
